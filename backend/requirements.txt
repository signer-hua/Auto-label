# ==============================================================================
# Auto-label 后端依赖（锁定版本，基于 Python 3.12 + CUDA 12.6 验证通过）
# ==============================================================================
#
# ⚠️ 安装顺序非常重要，请严格按以下步骤执行：
#
#   步骤0: 创建 conda 环境
#     conda create -n auto-label python=3.12 -y
#     conda activate auto-label
#
#   步骤1: 安装 PyTorch（必须先装，其他库依赖它）
#     pip install torch==2.7.0 torchvision==0.22.0 torchaudio==2.7.0 --index-url https://download.pytorch.org/whl/cu126
#
#   步骤2: 安装本文件中的依赖
#     pip install -r backend/requirements.txt
#
#   步骤3: 安装内置算法库（开发模式）
#     pip install -e backend/libs/dinov3
#     pip install -e backend/libs/sam3
#
#   步骤4: 前端依赖（另开终端）
#     cd frontend && npm install
#
# ==============================================================================

# ==================== 1. 基础工具 ====================
numpy==1.26.4                  # 必须 < 2.0（兼容 PyTorch 2.7 + pycocotools）
typing_extensions==4.15.0
tqdm==4.67.3

# ==================== 2. 图像处理 ====================
Pillow==12.0.0
opencv-python-headless==4.11.0.86
scipy==1.17.1

# ==================== 3. FastAPI 框架 ====================
fastapi==0.135.1
uvicorn[standard]==0.41.0      # 含 watchfiles/httptools/websockets
python-multipart==0.0.22
pydantic==2.12.5

# ==================== 4. Celery 异步任务队列 + Redis ====================
celery[redis]==5.6.2           # 含 redis/kombu/billiard/vine 等子依赖
redis==6.4.0

# ==================== 5. 机器学习通用 ====================
scikit-learn==1.8.0
scikit-image==0.26.0           # 实例分割后处理（regionprops）

# ==================== 6. LoRA 微调（可选，不装不影响基础标注功能） ====================
peft==0.18.1
accelerate==1.12.0

# ==================== 7. DINOv3 特征提取依赖 ====================
omegaconf==2.3.0
ftfy==6.1.1
regex==2026.2.28

# ==================== 8. SAM3 分割依赖 ====================
timm==1.0.25
huggingface_hub==1.5.0
pycocotools==2.0.11
psutil==7.2.2

# ==================== 9. Grounding DINO 检测依赖 ====================
transformers==5.2.0            # HuggingFace transformers（含 AutoProcessor）
einops==0.8.2
safetensors==0.7.0
tokenizers==0.22.2
