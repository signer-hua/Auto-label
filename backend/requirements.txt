# ==============================================================================
# Auto-label 后端依赖
# ==============================================================================
#
# 安装步骤：
#
#   步骤0: 安装 PyTorch（根据你的 CUDA 版本选择）
#     pip install torch==2.7.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
#
#   步骤1: 安装本文件中的依赖
#     pip install -r backend/requirements.txt
#
#   步骤2: 安装内置算法库
#     pip install -e backend/libs/dinov3
#     pip install -e backend/libs/sam3
#
# ==============================================================================

# ==================== 1. 基础工具 ====================
numpy>=1.26.0,<2
typing_extensions
tqdm

# ==================== 2. 图像处理 ====================
Pillow>=10.0.0
opencv-python-headless>=4.9.0
scipy>=1.12.0

# ==================== 3. FastAPI 框架 ====================
fastapi>=0.104.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.0.0

# ==================== 4. Celery 异步任务队列 + Redis ====================
celery[redis]>=5.3.0
redis>=5.0.0

# ==================== 5. 机器学习通用 ====================
scikit-learn>=1.4.0
scikit-image>=0.22.0

# ==================== 5.1 LoRA 微调（可选，不安装不影响基础功能） ====================
peft>=0.6.0
accelerate>=0.20.0

# ==================== 6. DINOv3 依赖 ====================
omegaconf
ftfy
regex

# ==================== 7. SAM3 依赖 ====================
timm>=1.0.17
huggingface_hub
pycocotools
psutil>=5.9.0

# ==================== 8. Grounding DINO 依赖（transformers 版，无 MM 系列依赖） ====================
transformers>=4.40.0
einops>=0.6.0
